package com.rifftech.temporal.collections;

/**
 * A NavigableTemporalPropertyCollection is a specialized collection that associates items of type {@code T}
 * with specific temporal ranges. It allows querying of items based on their temporal validity.
 *
 * @param <T> the type of elements in this temporal collection
 */
public class NavigableTemporalPropertyCollection<T> extends BaseTemporalCollection<T> implements TemporalPropertyCollection<T> {

}